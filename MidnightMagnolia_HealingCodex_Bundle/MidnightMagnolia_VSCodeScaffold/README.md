# Midnight Magnolia (Vite + React)

Install deps: `npm install`
Run dev server: `npm run dev`
Edit variables in `src/styles/variables.css`.
Docs in `/docs`.
Notion/Make in `/notion` and `/automations`.
