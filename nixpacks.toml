[providers]
node = "18"

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm"]
aptPkgs = []

[phases.install]
cmd = "npm ci"

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm run preview -- --host 0.0.0.0 --port $PORT"

[variables]
NODE_ENV = "production"